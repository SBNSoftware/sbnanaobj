////////////////////////////////////////////////////////////////////////
// \file    SRBlip.cxx
// \brief   SRBlip object for localized energy deposits in bulk LAr
// \author  $Author: jmclaughlin2@illinoistech.edu
////////////////////////////////////////////////////////////////////////

#include "sbnanaobj/StandardRecord/SRBlip.h"


namespace caf
{
    /*
    SRBlip::SRBlip()
    {
        ID              = -5;         // Blip ID / index
        isValid         = false;      // Blip passes basic checks
        Cryostat        = -5;         // Cryostat
        TPC             = -5;         // TPC
        NPlanes         = -5;         // Num. matched planes
        MaxWireSpan     = -5;         // Maximum span of wires on any plane cluster
        TimeTick        = -999;       // Readout time [ticks]
        Time            = -999;       // Drift time [us]
        Charge          = -5;         // Charge on calorimetry plane (electrons)
        Energy          = -5;       // Energy (const dE/dx, fcl-configurable)
        EnergyESTAR     = -5;       // Energy (ESTAR method from ArgoNeuT)
        EnergyPSTAR     = -5;       // Energy (PSTAR method similar with ESTAR method from ArgoNeuT)
        ProxTrkDist     = -5;         // Distance to cloest track
        ProxTrkID       = -5;         // ID of closest track
        inCylinder      = false;      // Is it in a cone/cylinder region? 
        Position.SetX(-999);          //3d X Position [cm]
        Position.SetY(-999);          //3d Y Position [cm]
        Position.SetZ(-999);          //3d Z Position [cm]
        SigmaYZ         = -5.;        // Uncertainty in YZ intersect [cm]
        dX              = -5;         // Equivalent length along drift direction [cm] 
        dYZ             = -5;         // Approximate length scale in YZ space [cm]
    }
    */

}
